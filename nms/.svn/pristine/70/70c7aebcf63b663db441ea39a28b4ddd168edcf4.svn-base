package com.kuaidu.nms.query.mapper;

import java.util.List;

import com.kuaidu.nms.entity.ReplaceConfig;

public interface ReplaceConfigMapper {

	List<ReplaceConfig> getAllConfigs(ReplaceConfig replaceConfig);

	int getCount(ReplaceConfig replaceConfig);

	void update_rConfig(ReplaceConfig replaceConfig);

	void del_rConfig(ReplaceConfig replaceConfig);

	void add_rConfig(ReplaceConfig replaceConfig);
	
}
